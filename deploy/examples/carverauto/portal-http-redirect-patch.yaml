# Plain HTTP on the LAN portal hostname exists only to send browsers to HTTPS.
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: firstmate-http-redirect
  namespace: firstmate
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: lan-shared-gateway
      namespace: lan-edge
      sectionName: http-carverauto
  hostnames:
    - firstmate.carverauto.dev
