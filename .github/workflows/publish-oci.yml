name: Publish OCI Images
on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      extra_tag:
        description: Optional extra tag (for example v0.1.0)
        required: false
        type: string
permissions:
  contents: read
  packages: write
jobs:
  publish:
    # Same self-hosted runners as ServiceRadar publish (captain directive).
    runs-on: firstmate-signing
    env:
      BUILDBUDDY_ORG_API_KEY: ${{ secrets.BUILDBUDDY_ORG_API_KEY || secrets.BUILDBUDDY_API_KEY }}
      OCI_REGISTRY: ${{ vars.OCI_REGISTRY || 'ghcr.io' }}
      OCI_PROJECT: ${{ vars.OCI_PROJECT || github.repository_owner }}
      BAZELISK_HOME: ${{ github.workspace }}/.cache/bazelisk
    steps:
      - uses: actions/checkout@v4
      # Bootstrap copied from the working serviceradar publish: cache the
      # bazelisk home (keyed by version + configs) so the Bazel distribution
      # download happens once per version, then install via script.
      - name: Cache Bazel artifacts
        uses: actions/cache@v4
        with:
          path: ${{ env.BAZELISK_HOME }}
          key: ${{ runner.os }}-bazel-${{ hashFiles('.bazelversion', '.bazelrc', 'MODULE.bazel') }}
          restore-keys: |
            ${{ runner.os }}-bazel-
      - name: Install Bazelisk
        run: ./scripts/install-bazelisk.sh
      - name: Verify Bazel version
        run: |
          set -euo pipefail
          want="$(tr -d '[:space:]' < .bazelversion)"
          # Bounded: a wedged distribution download fails here in minutes,
          # not as a silent hour-long hang in the push step.
          got="$(timeout 900 bazel --output_base=/tmp/fm-fm-port/bazel version 2>&1 | grep 'Build label' | awk '{print $3}')"
          echo "want=${want} got=${got}"
          test "${got}" = "${want}"
      - name: Configure BuildBuddy remote
        run: |
          set -euo pipefail

          if [ -z "${BUILDBUDDY_ORG_API_KEY:-}" ]; then
            echo "BUILDBUDDY_ORG_API_KEY is unavailable; --config=remote cannot authenticate." >&2
            exit 1
          fi

          umask 077
          printf 'common --remote_header=x-buildbuddy-api-key=%s\n' "${BUILDBUDDY_ORG_API_KEY}" > .bazelrc.remote
      - name: Login to the container registry
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          echo "${GITHUB_TOKEN}" | docker login "${OCI_REGISTRY}" -u "${GITHUB_ACTOR}" --password-stdin
      # Never print .bazelrc.remote (it holds the BuildBuddy API key header).
      - name: Diagnose Bazel remote reachability
        run: |
          set -euo pipefail
          getent hosts carverauto.buildbuddy.io || echo "WARN: carverauto.buildbuddy.io does not resolve"
          getent hosts cache-proxy.carverauto.dev || echo "WARN: cache-proxy.carverauto.dev does not resolve"
      - name: Push portal image
        env:
          EXTRA_TAG: ${{ github.event.inputs.extra_tag }}
          REF_NAME: ${{ github.ref_name }}
        run: |
          set -euo pipefail
          repo="${OCI_REGISTRY}/${OCI_PROJECT}/firstmate-port"
          tag="sha-$(git rev-parse --short HEAD)"
          echo "IMAGE_REPO=${repo}" >> "${GITHUB_ENV}"
          echo "IMAGE_TAG=${tag}" >> "${GITHUB_ENV}"
          bazel --output_base=/tmp/fm-fm-port/bazel run --config=remote //:portal_image_push -- --repository "${repo}" --tag "${tag}"
          if [[ "${GITHUB_REF:-}" == refs/tags/v* ]]; then
            bazel --output_base=/tmp/fm-fm-port/bazel run --config=remote //:portal_image_push -- --repository "${repo}" --tag "${REF_NAME}"
          fi
          if [ -n "${EXTRA_TAG}" ]; then
            bazel --output_base=/tmp/fm-fm-port/bazel run --config=remote //:portal_image_push -- --repository "${repo}" --tag "${EXTRA_TAG}"
          fi
      - name: Push fm-steer image
        env:
          EXTRA_TAG: ${{ github.event.inputs.extra_tag }}
          REF_NAME: ${{ github.ref_name }}
        run: |
          set -euo pipefail
          repo="${OCI_REGISTRY}/${OCI_PROJECT}/fm-steer"
          tag="sha-$(git rev-parse --short HEAD)"
          echo "STEER_IMAGE_REPO=${repo}" >> "${GITHUB_ENV}"
          echo "STEER_IMAGE_TAG=${tag}" >> "${GITHUB_ENV}"
          bazel --output_base=/tmp/fm-fm-port/bazel run --config=remote //:fm-steer_image_push -- --repository "${repo}" --tag "${tag}"
          if [[ "${GITHUB_REF:-}" == refs/tags/v* ]]; then
            bazel --output_base=/tmp/fm-fm-port/bazel run --config=remote //:fm-steer_image_push -- --repository "${repo}" --tag "${REF_NAME}"
          fi
          if [ -n "${EXTRA_TAG}" ]; then
            bazel --output_base=/tmp/fm-fm-port/bazel run --config=remote //:fm-steer_image_push -- --repository "${repo}" --tag "${EXTRA_TAG}"
          fi
      - name: Verify pushed manifests
        run: |
          set -euo pipefail
          for ref in "${IMAGE_REPO}:${IMAGE_TAG}" "${STEER_IMAGE_REPO}:${STEER_IMAGE_TAG}"; do
            digest="$(docker manifest inspect "${ref}" \
              | sha256sum | cut -d' ' -f1)"
            echo "manifest for ${ref} resolved (body sha256:${digest})"
          done
          docker manifest inspect "${IMAGE_REPO}:${IMAGE_TAG}" | head -40
